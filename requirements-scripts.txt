# Extra deps only needed for scripts/get_tokens.py (technical-account login).
pyrogram>=2.0.106
# tgcrypto is OPTIONAL: it only speeds up Pyrogram's crypto. Pyrogram works
# without it (falls back to pure-Python pyaes). Install it only if you have a
# C/C++ build toolchain; otherwise just skip this file's tgcrypto line.
tgcrypto>=1.2.5 ; platform_system != "Windows"
